name: demo workflow01

on:
  push:
  pull_request:

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install cowsay
        run: |
          sudo apt-get update
          sudo apt-get install -y cowsay

          cowsay -f dragon "run fast" > lion.txt

          cat lion.txt
          ls -ltra